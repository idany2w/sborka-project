{% from "../../components/stages-line/stages-line.nunj" import stagesLine %}

<div class="cart">
    <div class="cart__container">
        <div class="cart__inner">
            <div class="cart__content">

                {{ stagesLine({
                    items: cartStages,
                    acitveId: 3
                }) }}
            
            </div>
            <div class="cart__sidebar">cart__sidebar</div>
        </div>
    </div>
</div>